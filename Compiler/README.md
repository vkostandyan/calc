# alternative compiler


This `compiler/` folder contains a compact, independent implementation of a tiny expression compiler/REPL named `compiler`.

- Build: `make` (requires `g++`)
- Run: `./compiler` for REPL, or `./compiler "1 + 2 * 3"` to evaluate an expression.

Files are intentionally implemented with different structure and naming from the original `Compiler/` directory.
